# SOPS-encrypted — DO NOT edit plaintext values here.
# To rotate: decrypt, edit, re-encrypt, commit.
#
# Keys used by:
#   HelmRelease mariadb.auth.existingSecret: grimmory-secret
#     secretKeys.userPasswordKey:  MYSQL_PASSWORD
#     secretKeys.rootPasswordKey:  MYSQL_ROOT_PASSWORD
#
# Encrypt: cd clusters/staging && sops --encrypt --in-place ../../apps/base/grimmory/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: grimmory-secret
  namespace: grimmory
type: Opaque
stringData:
  MYSQL_PASSWORD: "REPLACE_AND_ENCRYPT"
  MYSQL_ROOT_PASSWORD: "REPLACE_AND_ENCRYPT"
